# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
.vercel
.tanstack
 
# Tauri / Rust build artifacts
src-tauri/target
src-tauri/.tauri

# Generated app icons (build/icon.png is the tracked source)
build/icon.icns
build/icon.ico

# wrangler files
.wrangler
# Cloudflare 本地开发变量文件（.dev.vars）始终忽略。
# 注意：如果以后要提交 .dev.vars.example 模板，请在此处补回 `!.dev.vars.example`，
# 并同时创建该文件——只有文件真实存在时否定规则才会生效。
.dev.vars*
.env*
!.env.example

# Build analysis artifacts
stats.html
build-report.json

# npm cache
.npm-cache

# local pnpm store fallback
.pnpm-store
