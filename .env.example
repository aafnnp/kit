# 复制为 .env 后按需填写。所有变量都必须是 VITE_ 前缀才会暴露给前端代码。

# Sentry DSN。留空则不会初始化 Sentry（见 src/lib/utils/error-handler.ts）。
# 注意：该值会被打包进前端产物，请只使用公开的 DSN。
VITE_SENTRY_DSN=

# VITE_APP_VERSION 不需要在这里配置，
# 它由 vite.config.ts 从 package.json 的 version 自动注入。
